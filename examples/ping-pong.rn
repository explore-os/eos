pub fn handle(state, msg) {
    state.count += 1;

    msg.state = match msg.state {
        "ping" => "pong",
        _ => "ping",
    };
    return (state, msg);
}

pub fn init() {
    return #{count: 0};
}
